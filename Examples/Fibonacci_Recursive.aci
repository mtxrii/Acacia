// Recursively prints the first 21 elements of the Fibonacci sequence. Slower but demonstrates more language features.

def fib(n) {
    if (n <= 1) return n;
    return fib(n - 2) + fib(n - 1);
}

for (let i = 0; i < 21; i++) {
    println(fib(i));
}
