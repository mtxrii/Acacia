
class Bagel {
    eat() {
        println("monch!");
    }

    spread(topping) {
        println("Added " + topping);
    }
}
let bagel = Bagel();
println(bagel);

bagel.topping = "cream cheese";

bagel.eat();
bagel.spread(bagel.topping);