def makeCounter() {
    let i = 0;
    def count() {
        i++;
        println(i);
    }

    return count;
}

let counter = makeCounter();
counter(); // "1".
counter(); // "2".

let x = [3, 5, 7];
let y = "Test";

println(len(x), len(y));